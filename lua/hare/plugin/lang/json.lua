return {
    -- This plugin provides a curated catalog of JSON (and YAML) schemas and makes them easy to use
    -- with Neovim's LSP configuration, especially jsonls and yamlls.
    --
    -- [https://github.com/b0o/SchemaStore.nvim]
    {
        'b0o/schemastore.nvim',
        lazy = true,
    },
}
